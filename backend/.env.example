# En produccion, solo la maquina que corre el programa deberia conocer el secret
SECRET=
ACCESS_MINUTES=60
COOKIE_SAMESITE=lax
COOKIE_SECURE=false

USUARIOS_A_RECOMENDAR=10
DEBUG=True
DOMINIO=
RESEND_TOKEN=
RESEND_EMAIL=
MONGODB=mongodb+srv

# === Multiprocessing ===
MP_START=fork

# === Arranque ===
HEAVY_ON_BOOT=false

# === Cadencias del scheduler (minutos) ===
CLOSE_EVERY_MIN=10
REL_EVERY_MIN=30
OPT_EVERY_MIN=60

# === Jitter (para no ejecutar siempre exacto al mismo segundo) ===
CLOSE_JITTER_S=15
REL_JITTER_S=30
OPT_JITTER_S=30

# === Timeouts y retries para jobs pesados ===
REL_TIMEOUT_S=900
REL_RETRIES=1
REL_BACKOFF_S=10

OPT_TIMEOUT_S=600
OPT_RETRIES=1
OPT_BACKOFF_S=10

# === Hilos del scheduler ===
SCH_THREADS=4
SCH_MISFIRE_GRACE=60

# === Algoritmo / performance ===
ENFORCE_PREFS_NOTIF=false
MAX_USERS_REL=0
OPTIMIZE_INSIDE_CLOSE=false

# === Notificaciones y slots ===
SLOT_CAPACITY=6
NOTIFY_OVERSUB_FACTOR=2.0
NOTIFY_MAX_CANDIDATES_PER_SLOT=12
NOTIFY_MIN_S=0.2
NOTIFY_MIN_A=0.0
NOTIFY_USER_COOLDOWN_MIN=30
NOTIFY_USER_MAX_PER_DAY=5
ENFORCE_PREFS_NOTIF=true

NOTIFY_ON_COUNTS=1,3,5
NOTIFY_COOLDOWN_MIN=60